#!/bin/sh
# /etc/local.d/ttyd.start

LOG="/var/log/ttyd.log"
LOG="/dev/null"

nc -w1 -n -z 127.0.0.1 7681 || killall "ttyd" 2>/dev/null
pgrep -x "ttyd" >/dev/null && exit

ttyd -i lo -p 7681 -m 2 -W -a busybox sh >> ${LOG} 2>&1 &
